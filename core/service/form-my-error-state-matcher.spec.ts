import { FormMyErrorStateMatcher } from './form-my-error-state-matcher';

describe('FormMyErrorStateMatcher', () => {
  it('should create an instance', () => {
    expect(new FormMyErrorStateMatcher()).toBeTruthy();
  });
});
